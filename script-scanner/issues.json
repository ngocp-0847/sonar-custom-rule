ImproperDataRetentionDetector: example-code-test/user_model.ts:18
  Entity/Model chứa thông tin cá nhân nhạy cảm (PII) nhưng không có cơ chế quản lý thời hạn lưu trữ
  Code: export class User {

ImproperDataRetentionDetector: example-code-test/user_model.ts:18
  Model chứa thông tin cá nhân nhạy cảm (PII) nhưng không có cơ chế quản lý thời hạn lưu trữ
  Code: export class User {

ImproperDataRetentionDetector: example-code-test/user_model.ts:63
  Entity/Model chứa thông tin cá nhân nhạy cảm (PII) nhưng không có cơ chế quản lý thời hạn lưu trữ
  Code: export interface UserProfile {

ImproperDataRetentionDetector: example-code-test/user_model.ts:81
  Model chứa thông tin cá nhân nhạy cảm (PII) nhưng không có cơ chế quản lý thời hạn lưu trữ
  Code: const userSchema = new mongoose.Schema({

ImproperDataRetentionDetector: example-code-test/user_model.ts:116
  Lưu trữ thông tin cá nhân nhạy cảm (PII) mà không thiết lập thời gian hết hạn
  Code: localStorage.setItem('userProfile', JSON.stringify(user));

ImproperDataRetentionDetector: example-code-test/user_model.ts:122
  Có thể ghi log thông tin nhạy cảm mà không có cơ chế xoay vòng hoặc xóa tự động
  Code: console.log(`User ${user.username} (SSN: ${user.socialSecurityNumber}) performed ${activity}`);

ImproperDataRetentionDetector: example-code-test/user_model.ts:0
  File chứa dữ liệu nhạy cảm nhưng không tìm thấy cơ chế tự động xóa dữ liệu cũ

ImproperDataRetentionDetector: example-code-test/example.php:0
  File chứa dữ liệu nhạy cảm nhưng không tìm thấy cơ chế tự động xóa dữ liệu cũ

